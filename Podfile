# Uncomment the next line to define a global platform for your project
platform :ios, '10.0'

target 'Period Flow 2' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Period Flow 2
  pod 'JTAppleCalendar', '~> 7.0'
  pod 'SwiftDate', '~> 4.0'
  pod 'PromiseKit', '~> 4.0'

  target 'Period Flow 2Tests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Period Flow 2UITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
